# 디노 글쓰기 코치 — 현재 상태

> **세션은 이 문서에서 시작합니다.** 1페이지를 넘기지 마세요 — 끝난 작업은 [`CHANGELOG.md`](CHANGELOG.md)로 내려보냅니다.
> 개발 규칙·명령어·함정은 [`../CLAUDE.md`](../CLAUDE.md), 화면별 동작 명세는 [`FEATURES.md`](FEATURES.md).

**마지막 갱신:** 2026-08-02
**저장소:** https://github.com/skyang9838-hue/dino-writing-coach (public)
**배포:** https://dino-writing-coach.vercel.app — 2026-08-01 확인 시점에 **`3ec2c40`(2026-07-28) 반영 상태**. 면담 보고서 파일럿과 수정 진행 대시보드는 아직 프로덕션에 없다(`master`에만 있음).

> ⚠️ **GitHub push로 Vercel 자동 배포가 걸리는지 불확실하다.** 2026-07-12에는 "안 걸린다"고 기록됐지만, 2026-08-01 확인 시 프로덕션이 직전 origin/master(`3ec2c40`)와 정확히 일치했다 — 자동 배포가 걸렸거나 누군가 수동 배포한 것이다. **배포 여부가 중요한 작업 전에는 Vercel 대시보드에서 실제 배포 목록을 직접 확인할 것.**

## 어디까지 왔나

교사가 활동을 만들고 학생이 참여 코드로 들어와 글을 쓰며 AI 코칭을 반복해 받는 교실 플랫폼. 프로덕션에 올라가 있다.

- **Phase 1** 기반 인프라 + 학생/교사 핵심 루프 — 완료
- **Phase 2** 교사의 학생별 수정 진행 보기 — 완료 (잔여: 학생 목록 정렬/검색, 필요해지면)
- **Phase 4** 무의미한 글 가드 + 욕설 감지 + 교사 승인 큐 — 완료
- **학생 글쓰기 수정 진행 대시보드** (2026-08-01~02, `816139c`~`3458734`) — 완료. 교사 화면이 라운드별 루브릭 판정과 직전 대비 변화를 보여준다. **실제 코칭 7라운드로 end-to-end 검증까지 끝났고**, 그 과정에서 코칭 파이프라인 버그를 하나 잡았다 ([CHANGELOG](CHANGELOG.md) 맨 아래)
- **Phase 3(루브릭)** — 범용 업로드 대신 **"면담 보고서" 파일럿으로 좁혀서 진행 중** ↓

## 진행 중: 면담 보고서 파일럿

루브릭을 교사가 업로드하는 범용 기능으로 만들기 전에, **한 장르에 루브릭을 고정해 넣어 코칭 품질을 먼저 검증**하는 파일럿. 2026-07-30~31에 26커밋 들어갔다.

**핵심 규칙 — 코칭에는 끝이 없다**

| 항목 | 규칙 |
|---|---|
| 첫 코칭 도달도 | 글 상태와 무관하게 **40%** |
| 이후 도달도 | 직전 미션 `done` 하나당 **+10%** (`partial`/`not-done`은 0%) |
| 상한 | **없음.** 감소도 없음. 막대 너비만 100%에서 멈추고 숫자는 계속 올라감 |
| 매 라운드 미션 | 항상 **정확히 2개.** 모든 기준이 `met`이어도 "더 다듬을" 미션을 채워서 2개 |
| 완료 상태 | **없음.** `complete`/"완성"/"완벽" 같은 제품 상태를 쓰지 않음 |
| 루브릭 백분율 | 학생에게 안 보임. 라운드의 `actualAttainment`로 교사 분석용 저장만 |

**구조**

- `lib/curriculum.js` — 면담 보고서 장르 + 루브릭 3개/채점기준 7개 (코드 상수, 교사 업로드 아님)
- `lib/coaching.js` — 2단계 Gemini 파이프라인: 판정(`buildInterviewAssessmentPrompt`) → 미션 생성(`buildInterviewMissionPrompt`). 진입점 `getInterviewReportFeedback`
- `lib/missions.js` — `selectMissionTargets`가 **AI가 아니라 코드로** 수정 대상 2개를 고름 (unmet→partial→우선순위, 최근 반복 대상 후순위, 짝 기준 병합)
- `lib/attainment.js` / `lib/interviewRound.js` — 누적 도달도 계산과 라운드 영속화
- `evals/` + `scripts/eval-interview-report.js` — 프롬프트 품질 라이브 평가

**남은 것 — 라이브 평가 (`2026-07-30-infinite-interview-coaching.md`의 Task 5)**

자동 테스트는 다 통과했지만 **Gemini 실호출 평가를 아직 안 돌렸다** (`.eval-results/` 없음). 남은 작업:

1. `npm run eval -- --set dev --runs 3` — 판정 일치율 90%+, 스키마 100%, 미션 2개 생성률 100%, 종료 상태 0건, 허위 충족·논리 모순 0건
2. `npm run eval -- --set validation --runs 1` — 일치율 85%+
3. 로컬 통합 확인 — 의미 있는 면담 보고서가 미션 2개와 40%를 받는지, 100%를 넘겨도 상한이 안 걸리는지, 화면에 완료 문구가 안 나오는지

**⚠️ 라이브 평가는 Gemini를 실제로 호출하므로 비용이 발생한다.** 돌리기 전에 사용자에게 확인할 것.

## 검증 상태 (2026-08-02)

- `npm test` **148개 통과**, `npm run lint` 무경고
- **수정 진행 대시보드는 실제 코칭 데이터로 검증 끝났다** — 상세는 [CHANGELOG](CHANGELOG.md) 맨 아래
- **Gemini 실호출 프롬프트 평가(`npm run eval`)는 여전히 미실행** — `.eval-results/` 없음. 위 검증은 보드/파이프라인 **동작** 검증이지 프롬프트 **품질** 평가가 아니다
- 로컬 DB에 검증용 활동 `VRFY01`(검증학생, 실제 코칭 7라운드)이 남아 있다

## 다음 할 일

### 1. 보드 가로 스크롤을 위로 (사용자 요청, 2026-08-02)

카드가 길어서 세로로 한참 내려가야 가로 스크롤바가 나온다. 카드 5장 이상이면 오른쪽 카드가 있다는 걸 알아채기도 어렵다. 스크롤 조작을 **카드 트랙 위쪽**으로 올린다. `.board-track`(`app/globals.css`)과 `components/RevisionBoard.jsx`.

### 2. ○△✕ 기호가 맞는지 재검토 (사용자 요청, 2026-08-02)

지금은 `○ 충분히 충족 / △ 보완 필요 / ✕ 미충족`이다. **이건 사람 교사가 확정 채점할 때 쓰는 기호인데, 실제로 판정한 건 AI다.** 틀릴 수 있다는 게 위 검증에서 실증됐다 — 같은 글을 두 번 판정했을 때 결과가 갈렸다. 확신을 덜 주는 기호(체크 / 물음표 / X 등)로 갈지 검토한다.

기호만 바꾸는 게 아니라 **화면이 주장하는 확실성의 수준**을 정하는 문제다. 하단 범례 문구(`충분히 충족`/`보완 필요`/`미충족`)도 같이 봐야 한다. `components/RevisionBoard.jsx`의 `RUBRIC_MARKS`와 `app/globals.css`의 `.rubric-mark-*`.

관련: 판정 잡음은 무변경 재코칭에서만 잡았다. **글이 실제로 바뀐 라운드의 `↑▼` 화살표에도 같은 잡음이 섞여 있을 수 있다.**

## 파일럿 이후 후보

- 면담 보고서에서 검증된 루브릭 코칭을 **다른 장르로 확장** (교사 업로드 방식으로 갈지, 장르별 고정 루브릭을 늘릴지 결정 필요)
- 채점기준표에 **AI 판단 근거** 붙이기 — 디자인 레퍼런스에는 "기준을 클릭하면 근거를 본다"가 있었지만 `INTERVIEW_ASSESSMENT_SCHEMA`에 근거 필드가 없어 뺐다. 넣으려면 프롬프트 수정 + 유료 라이브 평가 재실행
- **Gemini 호출 실패 원인이 로그에 안 남는다** — `lib/coaching.js`의 `fetch`를 감싼 `catch {}`가 에러를 통째로 삼키고 502로 바꾼다. 2026-08-02 검증 중 일시적 네트워크 실패가 났을 때 원인을 보려고 API를 따로 찔러봐야 했다. 운영에서 같은 일이 나면 단서가 없다
- **목표 글자 수는 1회차에만 검사한다** — `lib/actions.js`의 `requestCoaching`이 `isFirstRound`일 때만 길이를 본다. 2회차부터는 조건이 없어서, 학생이 1회차를 받은 뒤 글을 30자로 지워도 라운드가 계속 쌓인다. 의도된 설계였지만(`FEATURES.md` 6번) 실제로 열어보면 어색하다. 막으려면 서버(`requestCoaching`)와 클라이언트(`WritingScreen`) 양쪽을 고쳐야 한다
- Phase 2 잔여 — 참여 학생 목록 정렬/검색
- 가드 임계값(`lib/guard.js`)을 실제 학생 글 샘플로 튜닝

## 다음 세션 시작 프롬프트

```
dino-writing-coach 프로젝트를 이어서 작업합니다 (master 브랜치).
docs/PROJECT_STATUS.md에서 현재 상태를 확인해줘.

교사용 수정 진행 대시보드는 실제 코칭 데이터로 검증까지 끝났어.
다음은 "다음 할 일" 두 개야 — 보드 가로 스크롤을 위로 올리는 것,
그리고 채점기준표의 ○△✕가 AI 판정에 맞는 기호인지 재검토하는 것.
```
